package compiler.semantic.common;

/**
 * Simple type system for semantic analysis.
 */
public enum Type {
    UNKNOWN,
    INTEGER,
    FLOAT,
    BOOLEAN,
    STRING,
    LIST,
    DICTIONARY,
    NONE,
    FUNCTION
}
