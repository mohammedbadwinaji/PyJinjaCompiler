Prompt

You are working on an existing Java compiler project. Do NOT redesign the architecture. The project already compiles and the Python semantic analyzer is finished and working correctly.

The Jinja semantic analyzer is also working and already supports:

Undefined variable detection
Undefined function detection
Non-callable object detection
Binary expression type checking
For-loop iterable checking
Loop scopes
Shared SymbolTable with the Python analyzer

I only want to improve the existing implementation.

Important Rules
Do NOT change package names.
Do NOT rename classes.
Do NOT modify the AST structure.
Do NOT modify the parser.
Do NOT modify the lexer.
Do NOT modify PythonSemanticAnalyzer.
Do NOT modify SymbolTable, Symbol, Scope, Type unless absolutely required.
Do NOT introduce a new type system.
Keep compatibility with the current project.
Keep all existing semantic checks working exactly as they are.
Only extend the existing JinjaSemanticAnalyzer.

The goal is to make the semantic analyzer more complete without breaking any existing functionality.

Improvement 1 — Attribute Validation

Currently the analyzer only checks whether the target expression has UNKNOWN type.

Example:

{{ site_title.length }}

works because

site_title -> STRING

and the analyzer never validates whether length is a legal attribute.

Instead implement attribute validation.

Example allowed attributes:

STRING
length
upper
lower
strip
LIST
length
DICTIONARY
keys
values
items
length

If an attribute does not exist for the target type, report a semantic error.

Example:

{{ site_title.xyz }}

should produce something similar to

Unknown attribute 'xyz' for type STRING

Do not hardcode this logic inside visitAttributeAccess.

Instead create a helper such as

private boolean isValidAttribute(Type type, String attribute)

or something equivalent.

The implementation should be clean and easy to extend later.

Improvement 2 — If Condition Type Checking

Currently

visitJinjaIfStmt()

only visits the condition.

It never verifies the type.

Add semantic validation.

Allowed condition types:

BOOLEAN

Everything else should produce an error.

Examples:

Valid

{% if user_logged_in %}

Invalid

{% if 5 %}
{% if "hello" %}
{% if items_list %}

Report a semantic error such as

If condition must be BOOLEAN but found LIST

Do the same check for

JinjaElifClause
Improvement 3 — Index Validation

Currently

visitIndexAccess()

only checks whether the target is indexable.

Improve it.

Allowed targets:

LIST
STRING
DICTIONARY

Reject

INTEGER
FLOAT
BOOLEAN
FUNCTION
NONE

Also validate the index expression.

Rules:

LIST

index must be INTEGER

STRING

index must be INTEGER

DICTIONARY

allow any index for now
(do not redesign dictionary typing)

Examples:

Valid

items[0]
title[2]
dictionary["name"]

Invalid

items["abc"]
Index of LIST must be INTEGER

Invalid

title[True]
Index of STRING must be INTEGER
Improvement 4 — Built-in Function Signature Checking

Currently the analyzer only checks

function exists
callable

It never checks the number of arguments.

Add built-in signature validation.

Support at least these built-ins.

len

Exactly

1 argument

Examples

Valid

len(items)

Invalid

len()

Invalid

len(a,b)
range

Allow

range(stop)

range(start, stop)

range(start, stop, step)

Reject any other argument count.

Examples

Valid

range(5)
range(1,5)
range(1,10,2)

Invalid

range()
range(1,2,3,4)
str

Exactly

1 argument
int

Exactly

1 argument
float

Exactly

1 argument
bool

Exactly

1 argument

Create a helper similar to

private void validateBuiltinCall(CallExpr node, String functionName)

instead of placing everything inside visitCallExpr.

The implementation should be easy to extend with additional built-ins later.

Keep inferType Consistent

If new semantic checks require better type inference, extend

inferType(Expression expr)

without changing its overall design.

Do not redesign the type system.

Keep compatibility with the Python semantic analyzer.

Error Reporting

Reuse the existing

SemanticError

class.

Reuse existing ErrorType values whenever appropriate.

If the current ErrorType enum does not contain a suitable value for one of the new checks, add only the minimum new values necessary without changing existing ones.

Code Quality Requirements
Follow the current coding style used in the project.
Keep helper methods private.
Avoid duplicated logic.
Prefer helper methods over large visitor methods.
Do not remove existing comments.
Do not modify unrelated code.
Do not break compatibility with the Python semantic analyzer.
Ensure the project still compiles after the changes.
Preserve all existing behavior while adding the new semantic checks.

At the end, provide a summary of:

Every file modified.
Every helper method added.
Every semantic rule added.
Any new SemanticError.ErrorType values introduced.
Confirmation that no existing Python semantic analysis behavior was changed.